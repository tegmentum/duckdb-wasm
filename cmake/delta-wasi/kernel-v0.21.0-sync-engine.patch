diff --git a/ffi/Cargo.toml b/ffi/Cargo.toml
index e2cc784..0ba0757 100644
--- a/ffi/Cargo.toml
+++ b/ffi/Cargo.toml
@@ -62,3 +62,7 @@ default-engine-base = ["delta_kernel/default-engine-base"]
 tracing = [ "tracing-core", "tracing-subscriber" ]
 internal-api = []
 test-ffi = []
+# sync-engine: expose the kernel's local-filesystem engine through the FFI (for
+# reading local Delta tables on wasm). Reuses the ExternEngineVtable/engine_to_handle
+# plumbing that the default engine also uses (un-gated in src/lib.rs).
+sync-engine = ["delta_kernel/sync-engine", "delta_kernel/arrow"]
diff --git a/ffi/src/engine_data.rs b/ffi/src/engine_data.rs
index 621ea71..44f1c1d 100644
--- a/ffi/src/engine_data.rs
+++ b/ffi/src/engine_data.rs
@@ -1,22 +1,22 @@
 //! EngineData related ffi code
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use delta_kernel::arrow;
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use delta_kernel::arrow::array::{
     ffi::{FFI_ArrowArray, FFI_ArrowSchema},
     ArrayData, RecordBatch, StructArray,
 };
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use delta_kernel::engine::arrow_data::{ArrowEngineData, EngineDataArrowExt as _};
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use delta_kernel::DeltaResult;
 use delta_kernel::EngineData;
 use std::ffi::c_void;
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use crate::error::AllocateErrorFn;
 use crate::ExclusiveEngineData;
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 use crate::{ExternResult, IntoExternResult, SharedExternEngine};
 
 use super::handle::Handle;
@@ -56,14 +56,14 @@ unsafe fn get_raw_engine_data_impl(data: &mut Handle<ExclusiveEngineData>) -> &m
 /// Struct to allow binding to the arrow [C Data
 /// Interface](https://arrow.apache.org/docs/format/CDataInterface.html). This includes the data and
 /// the schema.
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 #[repr(C)]
 pub struct ArrowFFIData {
     pub array: FFI_ArrowArray,
     pub schema: FFI_ArrowSchema,
 }
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 impl ArrowFFIData {
     pub fn empty() -> Self {
         Self {
@@ -81,7 +81,7 @@ impl ArrowFFIData {
 /// # Safety
 /// data_handle must be a valid ExclusiveEngineData as read by the
 /// [`delta_kernel::engine::default::DefaultEngine`] obtained from `get_default_engine`.
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 #[no_mangle]
 pub unsafe extern "C" fn get_raw_arrow_data(
     data: Handle<ExclusiveEngineData>,
@@ -93,7 +93,7 @@ pub unsafe extern "C" fn get_raw_arrow_data(
 }
 
 // TODO: This method leaks the returned pointer memory. How will the engine free it?
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 fn get_raw_arrow_data_impl(data: Box<dyn EngineData>) -> DeltaResult<*mut ArrowFFIData> {
     let record_batch = data.try_into_record_batch()?;
     let sa: StructArray = record_batch.into();
@@ -115,7 +115,7 @@ fn get_raw_arrow_data_impl(data: Box<dyn EngineData>) -> DeltaResult<*mut ArrowF
 /// - `array` must be a valid FFI_ArrowArray
 /// - `schema` must be a valid pointer to a FFI_ArrowSchema
 /// - `engine` must be a valid Handle to a SharedExternEngine
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 #[no_mangle]
 pub unsafe extern "C" fn get_engine_data(
     array: FFI_ArrowArray,
@@ -125,7 +125,7 @@ pub unsafe extern "C" fn get_engine_data(
     get_engine_data_impl(array, schema).into_extern_result(&allocate_error)
 }
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 unsafe fn get_engine_data_impl(
     array: FFI_ArrowArray,
     schema: &FFI_ArrowSchema,
diff --git a/ffi/src/lib.rs b/ffi/src/lib.rs
index 680afee..b8a65fb 100644
--- a/ffi/src/lib.rs
+++ b/ffi/src/lib.rs
@@ -423,14 +423,14 @@ pub trait ExternEngine: Send + Sync {
 #[handle_descriptor(target=dyn ExternEngine, mutable=false)]
 pub struct SharedExternEngine;
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 struct ExternEngineVtable {
     // Actual engine instance to use
     engine: Arc<dyn Engine>,
     allocate_error: AllocateErrorFn,
 }
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 impl Drop for ExternEngineVtable {
     fn drop(&mut self) {
         debug!("dropping engine interface");
@@ -441,7 +441,7 @@ impl Drop for ExternEngineVtable {
 ///
 /// Kernel doesn't use any threading or concurrency. If engine chooses to do so, engine is
 /// responsible for handling  any races that could result.
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 unsafe impl Send for ExternEngineVtable {}
 
 /// # Safety
@@ -453,10 +453,10 @@ unsafe impl Send for ExternEngineVtable {}
 /// Basically, by failing to implement these traits, we forbid the engine from being able to declare
 /// its thread-safety (because rust assumes it is not threadsafe). By implementing them, we leave it
 /// up to the engine to enforce thread safety if engine chooses to use threads at all.
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 unsafe impl Sync for ExternEngineVtable {}
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 impl ExternEngine for ExternEngineVtable {
     fn engine(&self) -> Arc<dyn Engine> {
         self.engine.clone()
@@ -631,7 +631,7 @@ fn get_default_default_engine_impl(
 /// Safety
 ///
 /// Caller must free this handle to prevent memory leaks
-#[cfg(feature = "default-engine-base")]
+#[cfg(any(feature = "default-engine-base", feature = "sync-engine"))]
 fn engine_to_handle(
     engine: Arc<dyn Engine>,
     allocate_error: AllocateErrorFn,
@@ -643,6 +643,30 @@ fn engine_to_handle(
     engine.into()
 }
 
+/// Construct a kernel engine backed by the local-filesystem SYNC engine (no
+/// cloud/object_store/tokio). Used to read local Delta tables on wasm, where the
+/// default engine's async stack has no transport. The table location is supplied
+/// later at snapshot time, so this takes no path.
+///
+/// # Safety
+///
+/// Caller must free the returned handle to prevent memory leaks.
+#[cfg(feature = "sync-engine")]
+#[no_mangle]
+pub unsafe extern "C" fn get_sync_engine(
+    allocate_error: AllocateErrorFn,
+) -> ExternResult<Handle<SharedExternEngine>> {
+    get_sync_engine_impl(allocate_error).into_extern_result(&allocate_error)
+}
+
+#[cfg(feature = "sync-engine")]
+fn get_sync_engine_impl(
+    allocate_error: AllocateErrorFn,
+) -> DeltaResult<Handle<SharedExternEngine>> {
+    use delta_kernel::engine::sync::SyncEngine;
+    Ok(engine_to_handle(Arc::new(SyncEngine::new()), allocate_error))
+}
+
 /// Build the default engine
 ///
 /// If `executor_config` is `Some`, uses a multi-threaded executor that owns its runtime. Otherwise,
diff --git a/kernel/Cargo.toml b/kernel/Cargo.toml
index c6ae84d..46db067 100644
--- a/kernel/Cargo.toml
+++ b/kernel/Cargo.toml
@@ -64,9 +64,15 @@ futures = { version = "0.3", optional = true }
 reqwest = { version = "0.13", default-features = false, optional = true, features = ["charset", "http2", "system-proxy"] }
 # optionally used with default engine (though not required)
 tokio = { version = "1.47", optional = true, features = ["rt-multi-thread"] }
-# both arrow versions below are optional and require object_store
+# both arrow versions below are optional and require object_store.
+# wasm/sync: the cloud backends (aws/azure/gcp/http) pull reqwest+tokio, which have
+# no wasip2 transport. The sync engine only needs object_store's core path/types, so
+# build object_store_13 with no backends (object_store_12 is unused on the wasm path).
 object_store_12 = { package = "object_store", version = "0.12.3", optional = true, features = ["aws", "azure", "gcp", "http"] }
-object_store_13 = { package = "object_store", version = "0.13.1", optional = true, features = ["aws", "azure", "gcp", "http"] }
+object_store_13 = { package = "object_store", version = "0.13.1", optional = true }
+# the sync engine's json handler does atomic writes via a temp file (also a
+# dev-dependency below, but the sync handler needs it in non-test builds too)
+tempfile = { version = "3", optional = true }
 
 # arrow 57
 [dependencies.arrow_57]
@@ -89,7 +95,11 @@ optional = true
 [dependencies.parquet_58]
 package = "parquet"
 version = "58"
-features = ["async", "object_store"]
+# wasm/sync: drop the default zstd+brotli codecs (their bundled C symbols collide
+# with DuckDB's zstd + curl-wasm's libbrotli) and async/object_store (default-engine
+# only). The sync engine uses parquet's arrow reader with snappy (the Delta default).
+default-features = false
+features = ["arrow", "snap", "base64"]
 optional = true
 
 [features]
@@ -107,6 +117,10 @@ integration-test = ["hdfs-native-object-store/integration-test"]
 # The default versions for arrow/parquet/object_store
 arrow = ["arrow-58"] # latest arrow version
 need-arrow = [] # need-arrow is a marker that the feature needs arrow dep
+# sync-engine: the local-filesystem engine (no tokio/reqwest/cloud object_store),
+# used for reading local Delta tables on wasm. Re-exposes the otherwise test-only
+# sync module; needs arrow for its parquet/json handlers + tempfile for atomic writes.
+sync-engine = ["arrow", "need-arrow", "arrow-conversion", "arrow-expression", "dep:tempfile"]
 
 arrow-57 = ["dep:arrow_57", "dep:parquet_57", "dep:object_store_12"]
 arrow-58 = ["dep:arrow_58", "dep:parquet_58", "dep:object_store_13"]
diff --git a/kernel/src/engine/mod.rs b/kernel/src/engine/mod.rs
index 6e2da96..fa6f91f 100644
--- a/kernel/src/engine/mod.rs
+++ b/kernel/src/engine/mod.rs
@@ -28,7 +28,7 @@ pub(crate) fn writer_options() -> ArrowWriterOptions {
 #[cfg(feature = "arrow-conversion")]
 pub mod arrow_conversion;
 
-#[cfg(all(feature = "arrow-expression", feature = "default-engine-base"))]
+#[cfg(feature = "arrow-expression")]
 pub mod arrow_expression;
 #[cfg(all(feature = "arrow-expression", feature = "internal-api"))]
 pub mod arrow_utils;
@@ -40,18 +40,18 @@ pub use self::arrow_utils::{parse_json, to_json_bytes};
 #[cfg(feature = "default-engine-base")]
 pub mod default;
 
-#[cfg(test)]
-pub(crate) mod sync;
+#[cfg(any(test, feature = "sync-engine"))]
+pub mod sync;
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(feature = "need-arrow")]
 pub mod arrow_data;
-#[cfg(feature = "default-engine-base")]
+#[cfg(feature = "need-arrow")]
 pub(crate) mod arrow_get_data;
-#[cfg(all(feature = "default-engine-base", feature = "internal-api"))]
+#[cfg(all(feature = "need-arrow", feature = "internal-api"))]
 pub mod ensure_data_types;
-#[cfg(all(feature = "default-engine-base", not(feature = "internal-api")))]
+#[cfg(all(feature = "need-arrow", not(feature = "internal-api")))]
 pub(crate) mod ensure_data_types;
-#[cfg(feature = "default-engine-base")]
+#[cfg(feature = "need-arrow")]
 // module is always pub; trait inside is gated by #[internal_api]
 pub mod parquet_row_group_skipping;
 
diff --git a/kernel/src/engine/sync/mod.rs b/kernel/src/engine/sync/mod.rs
index 15a7e8a..5de508f 100644
--- a/kernel/src/engine/sync/mod.rs
+++ b/kernel/src/engine/sync/mod.rs
@@ -17,9 +17,9 @@ mod parquet;
 pub(crate) use parquet::SyncParquetHandler;
 mod storage;
 
-/// This is a simple (test-only) implementation of [`Engine`]. It only supports reading data from
+/// This is a simple implementation of [`Engine`]. It only supports reading data from
 /// the local filesystem, and internally represents data using `Arrow`.
-pub(crate) struct SyncEngine {
+pub struct SyncEngine {
     storage_handler: Arc<storage::SyncStorageHandler>,
     json_handler: Arc<json::SyncJsonHandler>,
     parquet_handler: Arc<parquet::SyncParquetHandler>,
@@ -27,7 +27,7 @@ pub(crate) struct SyncEngine {
 }
 
 impl SyncEngine {
-    pub(crate) fn new() -> Self {
+    pub fn new() -> Self {
         SyncEngine {
             storage_handler: Arc::new(storage::SyncStorageHandler {}),
             json_handler: Arc::new(json::SyncJsonHandler {}),
diff --git a/kernel/src/error.rs b/kernel/src/error.rs
index ee71deb..1a3242b 100644
--- a/kernel/src/error.rs
+++ b/kernel/src/error.rs
@@ -11,7 +11,7 @@ use crate::schema::{DataType, StructType};
 use crate::table_properties::ParseIntervalError;
 use crate::Version;
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(feature = "need-arrow")]
 use crate::arrow::error::ArrowError;
 #[cfg(feature = "default-engine-base")]
 use crate::object_store;
@@ -34,7 +34,7 @@ pub enum Error {
     },
 
     /// An error performing operations on arrow data
-    #[cfg(feature = "default-engine-base")]
+    #[cfg(feature = "need-arrow")]
     #[error(transparent)]
     Arrow(ArrowError),
 
@@ -69,7 +69,7 @@ pub enum Error {
     InternalError(String),
 
     /// An error enountered while working with parquet data
-    #[cfg(feature = "default-engine-base")]
+    #[cfg(feature = "need-arrow")]
     #[error("Arrow error: {0}")]
     Parquet(#[from] crate::parquet::errors::ParquetError),
 
@@ -355,7 +355,7 @@ from_with_backtrace!(
     (std::io::Error, IOError)
 );
 
-#[cfg(feature = "default-engine-base")]
+#[cfg(feature = "need-arrow")]
 impl From<ArrowError> for Error {
     fn from(value: ArrowError) -> Self {
         Self::Arrow(value).with_backtrace()
diff --git a/kernel/src/utils.rs b/kernel/src/utils.rs
index 48bc927..de9f3c1 100644
--- a/kernel/src/utils.rs
+++ b/kernel/src/utils.rs
@@ -40,6 +40,17 @@ pub(crate) fn try_parse_uri(uri: impl AsRef<str>) -> DeltaResult<Url> {
                     "{path:?} is not a directory"
                 )));
             }
+            // On wasm/WASI, std::fs::canonicalize does not produce a POSIX-absolute
+            // path, so Url::from_directory_path (which requires one) fails. The path
+            // is already relative to the host's WASI preopen, so just root it -- the
+            // host fs shim strips the leading '/' and resolves against the preopen.
+            #[cfg(target_family = "wasm")]
+            let path = if path.is_absolute() {
+                path
+            } else {
+                std::path::Path::new("/").join(&path)
+            };
+            #[cfg(not(target_family = "wasm"))]
             let path = std::fs::canonicalize(path).map_err(|err| {
                 let msg = format!("Invalid table location: {uri} Error: {err:?}");
                 Error::InvalidTableLocation(msg)
